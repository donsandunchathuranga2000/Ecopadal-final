<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#f5f5f5"
    android:gravity="center">

    <!-- ViewFlipper for image slideshow -->
    <ViewFlipper
        android:layout_width="wrap_content"
        android:layout_height="250dp"
        android:autoStart="true"
        android:flipInterval="3000"
        android:inAnimation="@android:anim/slide_in_left"
        android:outAnimation="@android:anim/slide_out_right"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="20dp">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:scaleType="centerInside"
            android:src="@drawable/bikesoft" />

        <!-- Additional images for the slideshow -->

    </ViewFlipper>


    <!-- Button layout below the ViewFlipper -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="bottom|center_horizontal"
        android:gravity="center"
        android:layout_marginTop="20dp"
        android:padding="16dp"
        android:background="#ffffff"
        android:elevation="4dp"
        android:layout_marginBottom="200dp">

        <Button
            android:id="@+id/selectDeviceButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Select Bluetooth Device"
            android:textColor="#ffffff"
            android:backgroundTint="#2196F3"
            android:textAllCaps="false"
            android:layout_marginBottom="16dp"
            android:elevation="2dp" />

        <Button
            android:id="@+id/UnlockButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Unlock"
            android:textColor="#ffffff"
            android:backgroundTint="#4CAF50"
            android:textAllCaps="false"
            android:layout_marginBottom="16dp"
            android:elevation="2dp" />

        <Button
            android:id="@+id/lockButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Lock"
            android:textColor="#ffffff"
            android:backgroundTint="#F44336"
            android:textAllCaps="false"
            android:elevation="2dp" />
    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="bottom|center_horizontal"
        android:gravity="center"
        android:layout_marginTop="20dp"
        android:padding="16dp"
        android:background="#ffffff"
        android:elevation="4dp"
        android:layout_marginBottom="30dp">

        <Button
            android:id="@+id/Starttimming"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Start the Journey"
            android:textColor="#ffffff"
            android:backgroundTint="#2196F3"
            android:textAllCaps="false"
            android:layout_marginBottom="16dp"
            android:elevation="2dp" />
    </LinearLayout>

</FrameLayout>
